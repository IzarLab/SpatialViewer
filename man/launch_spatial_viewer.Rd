% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{launch_spatial_viewer}
\alias{launch_spatial_viewer}
\title{Launch the Spatial Transcriptomics Viewer}
\usage{
launch_spatial_viewer(
  seurat_path = NULL,
  exclude_vars = NULL,
  exclude_patterns = NULL,
  x_col = NULL,
  y_col = NULL,
  reduction = NULL,
  assay = NULL,
  continuous_pals = NULL,
  polygon_path = NULL,
  cell_id_col = NULL,
  celltype_col = NULL,
  microns_per_pixel = NULL,
  config_path = if (file.exists("config.R")) "config.R" else NULL
)
}
\arguments{
\item{seurat_path}{Character or \code{NULL}. Path to a Seurat object saved as an
\code{.rds} file. If \code{NULL} (the default), the app opens with a file picker so
the dataset can be selected interactively.}

\item{exclude_vars}{Character vector. Metadata column names to omit from
display dropdowns. Default: \code{character(0)}.}

\item{exclude_patterns}{Character vector. Metadata columns whose names contain
any of these substrings are also excluded. Default: \code{character(0)}.}

\item{x_col}{Character. Metadata column for spatial X coordinates.
Default: \code{"x_centroid"}.}

\item{y_col}{Character. Metadata column for spatial Y coordinates.
Default: \code{"y_centroid"}.}

\item{reduction}{Character. Dimensionality reduction slot name (e.g. \code{"umap"}).
Auto-detects a fallback if the named slot is absent. Default: \code{"umap"}.}

\item{assay}{Character. Seurat assay name to read counts from.
Default: \code{"RNA"}.}

\item{continuous_pals}{Character vector of additional continuous palette names
to add to the UI dropdown. Default: \code{NULL}.}

\item{polygon_path}{Character or \code{NULL}. Path to a CSV or gzipped CSV of
cell boundary polygon vertices (columns: \code{cell}, \code{x_global_px},
\code{y_global_px}). Default: \code{NULL}.}

\item{cell_id_col}{Character or \code{NULL}. Seurat metadata column matching the
\code{cell} column in the polygon file. Default: \code{NULL}.}

\item{celltype_col}{Character or \code{NULL}. Metadata column pre-selected for
cell type grouping in the UI. Default: \code{NULL}.}

\item{microns_per_pixel}{Numeric or \code{NULL}. Conversion factor from coordinate
units to micrometres (e.g. \code{0.12} for CosMX Bruker pixel coordinates).
Used to label the scale bar in µm/mm. If \code{NULL} the scale bar is labelled
in raw coordinate units. Default: \code{NULL}.}
}
\value{
Called for its side effect: launches a blocking Shiny application.
}
\description{
Opens an interactive Shiny application for exploring a Seurat spatial
transcriptomics object. Supports coloring by metadata or gene expression,
spatial and dimensionality reduction views, polygon overlays, and a
Summary Statistics tab.
}
\examples{
\dontrun{
# Minimal usage
launch_spatial_viewer("path/to/seurat_object.rds")

# Custom coordinates + polygon overlay
launch_spatial_viewer(
  seurat_path  = "path/to/seurat_object.rds",
  x_col        = "x_slide_mm",
  y_col        = "y_slide_mm",
  reduction    = "scpearson_umap_nobatch",
  polygon_path = "path/to/polygons.csv.gz",
  cell_id_col  = "cell_ID"
)
}

}
